title: Action Lab
description: Emotional intelligence for young men trying to get focused, handle pressure, and turn self-awareness into action.
url: "https://www.actionlab.org" # change later to your real domain
baseurl: /Action-Lab # leave empty if the site lives at the root domain
timezone: America/New_York

markdown: kramdown
permalink: pretty

plugins:
  - jekyll-sitemap

exclude:
  - Gemfile
  - Gemfile.lock
  - README.md
  - node_modules
  - vendor
